<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Agregar Superhéroe</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-purple-900 via-gray-800 to-gray-900 text-white">
  <div class="container mx-auto px-6 py-10">
    <h1 class="text-4xl font-bold text-center mb-10 text-transparent bg-clip-text bg-gradient-to-r from-purple-500 to-pink-500">
      🦸 Agregar Superhéroe
    </h1>

    <% if (errores && errores.length > 0) { %>
      <div class="bg-red-600 text-white px-4 py-3 rounded-lg mb-6 shadow-md">
        <ul>
          <% errores.forEach(error => { %>
            <li>⚠️ <%= error %></li>
          <% }); %>
        </ul>
      </div>
    <% } %>

    <form action="/api/heroes/agregar" method="POST" class="bg-gray-800 p-6 rounded-lg shadow-lg">
      <div class="mb-4">
        <label for="nombreSuperHeroe" class="block text-sm font-medium text-pink-400">Nombre del Superhéroe</label>
        <input type="text" id="nombreSuperHeroe" name="nombreSuperHeroe" class="mt-1 p-3 block w-full bg-gray-700 text-white rounded focus:ring-2 focus:ring-pink-500" required>
      </div>
      <div class="mb-4">
        <label for="nombreReal" class="block text-sm font-medium text-pink-400">Nombre Real</label>
        <input type="text" id="nombreReal" name="nombreReal" class="mt-1 p-3 block w-full bg-gray-700 text-white rounded focus:ring-2 focus:ring-pink-500" required>
      </div>
      <div class="mb-4">
        <label for="edad" class="block text-sm font-medium text-pink-400">Edad</label>
        <input type="number" id="edad" name="edad" class="mt-1 p-3 block w-full bg-gray-700 text-white rounded focus:ring-2 focus:ring-pink-500" required min="0">
      </div>
      <div class="mb-4">
        <label for="planetaOrigen" class="block text-sm font-medium text-pink-400">Planeta de Origen</label>
        <input type="text" id="planetaOrigen" name="planetaOrigen" class="mt-1 p-3 block w-full bg-gray-700 text-white rounded focus:ring-2 focus:ring-pink-500" required>
      </div>
      <div class="mb-4">
        <label for="debilidad" class="block text-sm font-medium text-pink-400">Debilidad</label>
        <input type="text" id="debilidad" name="debilidad" class="mt-1 p-3 block w-full bg-gray-700 text-white rounded focus:ring-2 focus:ring-pink-500" required>
      </div>
      <div class="mb-4">
        <label for="poderes" class="block text-sm font-medium text-pink-400">Poderes (separados por comas)</label>
        <textarea id="poderes" name="poderes" class="mt-1 p-3 block w-full bg-gray-700 text-white rounded focus:ring-2 focus:ring-pink-500" required></textarea>
      </div>
      <div class="mb-4">
        <label for="aliados" class="block text-sm font-medium text-pink-400">Aliados (separados por comas)</label>
        <textarea id="aliados" name="aliados" class="mt-1 p-3 block w-full bg-gray-700 text-white rounded focus:ring-2 focus:ring-pink-500"></textarea>
      </div>
      <div class="mb-4">
        <label for="enemigos" class="block text-sm font-medium text-pink-400">Enemigos (separados por comas)</label>
        <textarea id="enemigos" name="enemigos" class="mt-1 p-3 block w-full bg-gray-700 text-white rounded focus:ring-2 focus:ring-pink-500"></textarea>
      </div>
      <div class="flex justify-between">
        <button type="submit" class="bg-gradient-to-r from-pink-500 to-orange-500 hover:from-pink-600 hover:to-orange-600 text-white font-bold py-2 px-6 rounded-lg shadow-lg">
          + Agregar
        </button>
        <a href="/api/heroes" class="bg-gray-700 hover:bg-gray-800 text-white font-bold py-2 px-6 rounded-lg shadow-lg">
          Volver
        </a>
      </div>
    </form>
  </div>
</body>
</html>